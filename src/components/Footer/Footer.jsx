import React from 'react';
import "../../Sass/style.scss";

const Footer = (props) => {
    return (
        <footer className="footer">
            Footer
        </footer>
    );
}

export default Footer;